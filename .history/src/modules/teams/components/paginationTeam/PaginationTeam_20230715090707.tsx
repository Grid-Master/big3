import { FC } from 'react';
import ReactPaginate from 'react-paginate';

const PaginationTeam = () => {
  const handlePageClick = () => {};
  return <ReactPaginate breakLabel="..." nextLabel=">" onPageChange={} />;
};

export default PaginationTeam;
