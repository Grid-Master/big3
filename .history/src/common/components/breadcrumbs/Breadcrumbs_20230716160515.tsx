import { FC } from 'react';
import styles from './breadcrumbs.module.sass';

const Breadcrumbs: FC = () => {
  return <p className={styles.breadcrumbs}>Teams / Denver Nuggets</p>;
};

export default Breadcrumbs;
